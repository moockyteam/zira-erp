// app/dashboard/returns/print/layout.tsx

import type React from "react"

// Layout nu pour la page d'impression
export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}